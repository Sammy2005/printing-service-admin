import { PrinterTrayPipe } from './printer-tray.pipe';

describe('PrinterTrayPipe', () => {
  it('create an instance', () => {
    const pipe = new PrinterTrayPipe();
    expect(pipe).toBeTruthy();
  });
});
